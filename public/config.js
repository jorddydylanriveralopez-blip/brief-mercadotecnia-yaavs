/**
 * Configuración del formulario Brief Mercadotecnia.
 * El envío principal va al servidor (/api/submit).
 * Google Sheets se conecta vía SHEETS_WEBHOOK_URL en el servidor
 * (ver gas/Code.gs).
 */
window.YAAVS_BRIEF_CONFIG = {
  mode: "local",
  formAction: "",
  entryId: "",
  sheetUrl: "",
};
